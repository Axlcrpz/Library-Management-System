# Uploaded profile photos are served only through
# api/library_handler.php?action=user_avatar (auth-checked). Block direct access.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
